# FastAPI and server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# HTTP client for Vast.ai API calls
aiohttp==3.9.1

# Image processing
Pillow==10.1.0
numpy==1.24.3

# Note: No PyTorch or SDXL models needed!
# This backend calls Vast.ai Stable Diffusion API instead of loading models locally.
# The SD models run on the Vast.ai GPU server via their API.
